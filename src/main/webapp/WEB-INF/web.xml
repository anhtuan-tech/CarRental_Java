<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
   version="6.0">

    <!-- Session timeout: 30 minutes -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- Default welcome file routes to HomeController -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!--
        All servlet URL mappings are handled via @WebServlet annotations:
          HomeController              → /home
          LoginCustomerController     → /login/customer
          LoginStaffController        → /login/staff
          LoginOwnerController        → /login/owner
          RegisterCustomerController  → /register/customer
          RegisterOwnerController     → /register/owner
          LogoutController            → /logout

        UTF-8 encoding is handled per-request via request.setCharacterEncoding("UTF-8")
        inside each controller's doPost() – no servlet Filter needed.
    -->

</web-app>
